var mainWin = Ti.UI.createWindow({
	title: "Hierarchy",
	backgroundColor: "#FFF"
});

var label1 = Ti.UI.createLabel({
	text: "40",
	font: {
		fontSize: 100,
		fontFamily: "Helvetica Light",
	}
});

mainWin.add(label1);
mainWin.open();
