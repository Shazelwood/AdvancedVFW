var mainWin = Ti.UI.createWindow({
	title: "Hierarchy",
	backgroundColor: "#FFF"
});

var label1 = Ti.UI.createLabel({
	text: "40 Helvetica Light"
	color: "black",
	font: {
		fontSize: 40,
		fontFamily: "Helvetica Light",
	}
});

mainWin.add(label1);
mainWin.open();
